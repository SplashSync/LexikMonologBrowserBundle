# Framework Configuration
framework:
    translator:      { fallbacks: ["%locale%"] }
    router:
        resource:   "%kernel.root_dir%/routing.yml"
    templating:
        engines: ['twig']  
    form:           ~ 
    test: ~

